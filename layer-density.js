/* ===============================
   ENTRIXX — DENSITY LAYER (MINIMAL)
   =============================== */

// Абсолютно безопасная заглушка.
// Не читает данные.
// Не трогает canvas напрямую.
// Не может сломать рендер.

(function () {
  window.drawDensityLayer = function () {
    // intentionally empty
  };
})();
